{
  "name": "lambda-discord-bot",
  "version": "0.0.0",
  "scripts": {
    "dev": "ENV=development ts-node src/dev.ts",
    "build": "tsc && (cd dist && zip -r dist.zip .)",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex Baumhoer",
  "license": "None",
  "devDependencies": {
    "@tsconfig/node14": "^1.0.1",
    "@types/node": "^16.3.0",
    "dotenv": "^10.0.0",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5"
  }
}
